/*
 * Copyright (c) 2026 Microchip Technology Inc.
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * Row A of the pulses-per-round Latin square. Combined with the tach-edges
 * values fixed in app.overlay this build covers (2, 1), (3, 2), (5, 3), (9, 4).
 * Apply with EXTRA_DTC_OVERLAY_FILE so that app.overlay still takes effect.
 */

&tach0 {
	pulses-per-round = <1>;
};

&tach1 {
	pulses-per-round = <2>;
};

&tach2 {
	pulses-per-round = <3>;
};

&tach3 {
	pulses-per-round = <4>;
};
