/*
 * Copyright (c) 2026 Microchip Technology Inc.
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * Row C of the pulses-per-round Latin square. Combined with the tach-edges
 * values fixed in app.overlay this build covers (2, 3), (3, 4), (5, 1), (9, 2).
 * Apply with EXTRA_DTC_OVERLAY_FILE so that app.overlay still takes effect.
 */

&tach0 {
	pulses-per-round = <3>;
};

&tach1 {
	pulses-per-round = <4>;
};

&tach2 {
	pulses-per-round = <1>;
};

&tach3 {
	pulses-per-round = <2>;
};
